# Adaptive_Uart
Adaptive UART is a Verilog-based communication system supporting parallel and serial data transmission. It converts selected input data into UART frames, transmits and receives the data, and tracks each transmitted serial bit using a bit counter for clear monitoring and analysis in simulation.
